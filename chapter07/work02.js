// オブジェクトの値取得
const person = {
    name : 'Elsa',
    age : 15,
    hobby : '魔法使い',
    address : "東京",
};

// ドット記法
console.log( person.name );
console.log( person.age );

// 角カッコ記法
console.log( person['hobby'] );
console.log( person['address'] );
